server:
  port: 8001

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:com/family/handleLog/mapper/xml/*.xml

spring:
  application:
    name: common-handle-log
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #服务注册中心地址
      config:
        server-addr: 47.120.9.227:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置
        namespace: 57847352-9735-4e6e-b9eb-69d55be313af
        group: DEV_GROUP

management:
  endpoints:
    web:
      exposure:
        include: '*'
